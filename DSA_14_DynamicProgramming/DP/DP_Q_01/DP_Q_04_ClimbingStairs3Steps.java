package DSA_14_DynamicProgramming.DP.DP_Q_01;

public class DP_Q_04_ClimbingStairs3Steps {
    
    // either next or skip next or skip next next
    // no of ways of reaching n
    // f(n) = f(n-1) + f(n-2) + f(n-3) 
    // statement on tribbonacci numbers

    // same as earlier

    // recursion
    // memoization
    // tabulation
    // iteration (space optimised tabulation)
    // matrix exponentiation
}
